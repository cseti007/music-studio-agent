__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/

# audio working files
*.wav
*.flac
*.mp3
*.aiff
*.ogg
output/
processed/
spectrograms/

# analysis output
*.json
*.png

# but keep instrument presets — these ship with the repo
!tools/presets/*.json

# and keep the style profiles — these ship with the repo
!tools/style_profiles/*.json

# and keep the synthetic IR pack — ships with the repo
!tools/irs/*.wav

# Claude Code local settings (machine-specific)
.claude/settings.local.json
